#include "../include/solver_sequential.h"

void solver(int* matix, int dimension, char* file_path) {
    for (int i = 0; i < dimension; i++) {
        int cont = 0;
        for (int j = i + 1; j < dimension; j++) {
            
        }
    }
}